# Minimal dependencies for modem baking, runtime and reception.
# Included by requirements.txt; also usable alone for standalone modem tools.
# sounddevice's native PortAudio library is installed by the OS package manager.
numpy>=1.24
numba>=0.61
scipy>=1.10
Pillow>=9.5
sounddevice>=0.4.6
pyautogui
pynput
